<div class="col-md-8 offset-md-1">
  <table class="table">
  <h1>FollowUser</h1>
  <% if @followings.empty? %>
     <span>ユーザーはいません</span>
  <% else%>
    <thead>
      <tr>
        <th>name</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tboy>
    <div class="col-md-8 offset-md-1">
      <% @followings.each do |user| %>
        <tr>
          <th>  <%= link_to user.name, user_path(user.id) %> </td>
          <th><%= "フォロー数: #{user.follower.count}" %></th>
          <th><%= "フォロワー数: #{user.followed.count}" %></th>
        </tr>
    </div>
      <% end %>
  <% end %>
  </table>
</div>