 <th>follows<%= link_to " #{@user.follower.count}", user_followsings_path(@user) %></th>
            </tr>
            <tr>
              <th>followers:<%= link_to " #{@user.followed.count}", user_followers_path(@user) %></th>
            </tr>
            <tr>
              <th>
                <% if current_user.following?(@user) %>
                <%= link_to 'フォロー外す', unfollow_path(@user.id), method: :POST, class: "btn btn-info" %>
                <% else %>
                <%= link_to 'フォローする', follow_path(@user.id), method: :POST, class: "btn btn-success" %>
                <% end %>
              </th>
            </tr>